# Dev/test tooling (run tests without Docker). Service deps live in
# ingest/requirements.txt and dashboard/requirements.txt.
pyyaml==6.0.2
pytest==8.3.4
sqlalchemy==2.0.51
alembic==1.14.0
# Optional: only needed to run the Postgres migration test (test_migrations.py)
# against a real DB via CENSUS_TEST_DATABASE_URL. SQLite tests don't need it.
psycopg[binary]>=3.2
